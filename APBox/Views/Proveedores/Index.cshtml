@model IEnumerable<API.Catalogos.Proveedor>

<div class="card card-flush shadow-sm py-4">
    <!--begin::Card header-->
    <div class="card-header">
        <!--begin::Search-->
        <div class="d-flex align-items-center position-relative my-1">
            <i class="fa fa-magnifying-glass position-absolute ms-4"></i>
            <input type="text" data-kt-filter="search" class="form-control form-control-solid w-250px ps-14" placeholder="Buscar" />
        </div>
        <!--end::Search-->
        <div class="card-toolbar flex-row-fluid justify-content-end gap-5">
            <!--begin::Export dropdown-->
            <button type="button" class="btn btn-light-primary" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end">
                <i class="fa fa-file-export"></i>
                Exportar
            </button>
            <!--begin::Menu-->
            <div id="kt_datatable_example_export_menu" class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg-light-primary fw-semibold fs-7 w-200px py-4" data-kt-menu="true">
                <!--begin::Menu item-->
                <div class="menu-item px-3">
                    <a href="#" class="menu-link px-3" data-kt-export="copy">
                        Copiar a portapapeles
                    </a>
                </div>
                <!--end::Menu item-->
                <!--begin::Menu item-->
                <div class="menu-item px-3">
                    <a href="#" class="menu-link px-3" data-kt-export="excel">
                        Exportar a Excel
                    </a>
                </div>
                <!--end::Menu item-->
                <!--begin::Menu item-->
                <div class="menu-item px-3">
                    <a href="#" class="menu-link px-3" data-kt-export="csv">
                        Exportar a CSV
                    </a>
                </div>
                <!--end::Menu item-->
                <!--begin::Menu item-->
                <div class="menu-item px-3">
                    <a href="#" class="menu-link px-3" data-kt-export="pdf">
                        Exportar a PDF
                    </a>
                </div>
                <!--end::Menu item-->
            </div>
            <!--end::Menu-->
            <!--end::Export dropdown-->
            <!--begin::Hide default export buttons-->
            <div id="kt_datatable_example_buttons" class="d-none"></div>
            <!--end::Hide default export buttons-->

        </div>
    </div>
    <!--end::Card header-->
    <!--begin::Card body-->
    <div class="card-body pt-0">
        <div class="d-flex flex-column gap-10">
            <table class="table align-middle rounded table-row-dashed fs-6 g-5" id="kt_datatable">
                <thead>
                    <tr class="text-start text-gray-600 fw-bold fs-7 text-uppercase">
                        <th class="min-w-100px">
                            Socio Comercial
                        </th>
                        <th class="min-w-100px">
                            Contacto
                        </th>
                        <th class="min-w-100px">
                            Fecha de alta
                        </th>
                        <th class="min-w-100px">
                            Estado
                        </th>
                        <th class="min-w-100px">

                        </th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.RazonSocial) <br />
                                                                            <span class="text-gray-600">@Html.DisplayFor(modelItem => item.Rfc)</span>
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                            <br />
                            <div class="text-gray-600">
                                @if (!string.IsNullOrEmpty(item.Telefono1))
                                {
                                    <span class="badge"> Teléfono 1: </span> @Html.DisplayFor(modelItem => item.Telefono1)<br />
                                }
                                @if (!string.IsNullOrEmpty(item.Telefono2))
                                {
                                    <span class="badge"> Teléfono 2: </span> @Html.DisplayFor(modelItem => item.Telefono2)
                                }
                            </div>

                        </td>
                        <td>
                            @item.FechaAlta.ToString("dd/MM/yyyy")
                        </td>
                        <td>
                            @if (item.Status == API.Enums.Status.Activo)
                            {
                                <div class="badge badge-light-success">Activo</div>
                            }
                            else
                            {
                                <div class="badge badge-light-danger">Inactivo</div>
                            }
                        </td>
                        <td class="text-end">
                            <!--begin::Action=-->
                            <a href="#" class="btn btn-sm btn-light btn-flex btn-center btn-active-light-primary" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end">
                                Acciones
                                <i class="ki-duotone ki-down fs-5 ms-1"></i>
                            </a>
                            <!--begin::Menu-->
                            <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg-light-primary fw-semibold fs-7 py-4 w-auto" data-kt-menu="true">
                                @if (User.IsInRole("EDICION"))
                                {
                                    <div class="menu-item px-3">@Html.ActionLink("Edit Pagos", "Edit", new { id = item.Id }, new { @class = "menu-link px-3" })</div>

                                }
                                @if (User.IsInRole("BORRADO"))
                                {
                                    <div class="menu-item px-3">@Html.ActionLink("Eliminar", "Delete", new { id = item.Id }, new { onclick = "return confirm('¿Estás seguro de elminar el complemento?');", @class = "menu-link px-3" })</div>
                                }
                            </div>
                            <!--end::Menu-->
                            <!--end::Action=-->
                        </td>
                    </tr>
                }


            </table>
        </div>
    </div>
    <!--end::Card header-->
</div>


<div class="hiddenDiv">
    <div class="d-flex align-items-center py-3 py-md-1" id="tools">
        <!--begin::Wrapper-->
        <!--begin::Button-->
        <a href="@Url.Action("Create","Proveedores")" class="btn btn-secondary">
            Nuevo
        </a>
        <!--end::Button-->

    </div>
</div>